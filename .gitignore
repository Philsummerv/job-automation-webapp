node_modules/
.next/
out/
dist/
.turbo/
*.log
.DS_Store

# Build artifacts / generated
*.tsbuildinfo
next-env.d.ts

# Env files (never commit secrets)
.env
.env.local
.env*.local
apps/*/.env
apps/*/.env.local

# Supabase local
.supabase/

# Automation driver — personal details + run screenshots (never commit)
packages/automation/driver.config.json
packages/automation/artifacts/

# Captured page HTML dumps used for debugging the extension scraper
indeed_html/

# Saved page dumps for debugging — contain session/tracking tokens
*-html.txt

# Personal UI claim documents — benefit amounts, address, financials, claim
# correspondence. Never commit these; keep them outside the repo entirely.
private/
*-ui-summary.md
*claim-summary*.md

# Chrome Web Store upload package — a build artifact, regenerate with
# `node build.mjs --store` in apps/extension and zip the contents of dist/.
# NOTE: apps/web/public/jobassistui-extension.zip is NOT ignored — that one is
# served by the website and has to ship with the deploy.
apps/extension/jobassistui-store.zip
